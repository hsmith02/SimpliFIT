import React from 'react';
import { Button } from 'react-toolbox/lib/button';
import theme from '../styles/Button.css';

const AppButton = (props) => <Button {...props} theme={theme} />;
export default AppButton;
